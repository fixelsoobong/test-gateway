spring:
  cloud:
    gateway:
      default-filters:
        - TokenRelay
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: 
              - "*"            
            allowedMethods:
              - GET
              - POST
      httpclient:
        wiretap: true
        connect-timeout: 30000
        response-timeout: 30s
      httpserver:
        wiretap: true
      routes:
        - id: mysc-api-instance
          uri: http://localhost:9998/
          predicates:
            - Path=/mysc/**
          filters:
            - CustomAuthFilter
            - name: RequestSize
              args: 
                maxSize : 10000000
            - name: Hystrix
              args:
                name: vc-api-fallback
                fallbackuri: forward:/vcApiFallBack
        - id: vse_app
          uri: http://localhost:8710/
          predicates:
            - Path=/vse_app/**
          filters:            
            - CustomNewAuthFilter
            - name: RequestSize
              args: 
                maxSize : 7000000
            - name: Hystrix
              args:
                name: vc-api-fallback
                fallbackuri: forward:/vcApiFallBack
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone : http://admin:admin@172.31.5.58:8761/eureka/
  instance:
    hostname: localhost
